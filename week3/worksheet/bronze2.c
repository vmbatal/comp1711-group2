#include <stdio.h>

int main() {

    int num = 16;
    int b = "%d"

    if ((num/4) = "%d",b && (num/5) = "%d")
    {
        printf ("%d is divisible by 4 and 5\n", num);
    }
    else
    {
        printf ("%d is not divisible by 4 and 5\n", num);
    }

    return 0;
}